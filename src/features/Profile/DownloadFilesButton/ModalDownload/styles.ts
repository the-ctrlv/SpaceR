import { styled } from 'styled-components';

import SpaceModal from 'components/SpaceModal';

export const StyledSpaceModal = styled(SpaceModal)`
  header {
    display: none;
  }
  svg {
    display: none;
  }
`;
